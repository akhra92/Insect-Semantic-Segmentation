# Local GPU training environment (Python 3.12).
# Versions match requirements.txt so checkpoints trained here load in the
# Streamlit app unchanged -- only the torch build differs (CUDA vs CPU).
#
#   conda create -n insect python=3.12 -y && conda activate insect
#   pip install -r requirements-train.txt

--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.2.2+cu121
torchvision==0.17.2+cu121

segmentation-models-pytorch==0.3.4

albumentations==1.4.18
opencv-python-headless==4.10.0.84
Pillow==10.4.0
numpy==1.26.4
matplotlib==3.9.2
tqdm==4.66.1

# Dataset download
kaggle>=1.6.0
